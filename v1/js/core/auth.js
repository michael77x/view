(function () {   const refs = window.HiviAppDOM;   const ui = window.HiviAppUI;    function formatName(value) {     return value       .split(" ")       .filter(Boolean)       .map((part) => part.charAt(0).toUpperCase() + part.slice(1).toLowerCase())       .join(" ");   }    function setAuthMode(mode) {     refs.authModeButtons.forEach((button) => {       button.classList.toggle("is-active", button.dataset.authMode === mode);     });      refs.authForms.forEach((form) => {       form.classList.toggle("is-visible", form.id === `${mode}-form`);     });   }    function setUser(name) {     const normalized = formatName(name || "Cliente Hivimar");     const firstName = normalized.split(" ")[0];     refs.homeName.textContent = firstName;     refs.doneClient.textContent = normalized;     refs.profileName.textContent = normalized;     ui.updateProfileAvatar(normalized);   }    function logout() {     ui.setAuthenticated(false);     setAuthMode("login");     ui.setScreen("login");   }    window.HiviAppAuth = {     setAuthMode,     setUser,     logout,   }; })();