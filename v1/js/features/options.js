(function () {   const tools = window.HiviAppData.tools;   const refs = window.HiviAppDOM;   const ui = window.HiviAppUI;    function renderTools() {     refs.toolList.innerHTML = tools       .map(         (tool) => `           <article class="tool-card tool-card--active" data-tool-screen="${tool.screen}">             <strong>${tool.title}</strong>             <p>${tool.description}</p>           </article>         `,       )       .join("");      refs.toolList.querySelectorAll("[data-tool-screen]").forEach((card) => {       card.addEventListener("click", () => {         ui.setScreen(card.dataset.toolScreen);       });     });   }    window.HiviAppOptions = { renderTools }; })();