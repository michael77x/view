(function () {   const lines = window.HiviAppData.lines;   const refs = window.HiviAppDOM;   const state = window.HiviAppState;   const ui = window.HiviAppUI;   const lineFeature = window.HiviAppLine;   const requestFeature = window.HiviAppRequestForm;    function renderHome() {     refs.lineList.innerHTML = lines       .map(         (line) => `           <article class="line-card" data-line-id="${line.id}">             <strong class="line-card-title">${line.title}</strong>             <p>${line.summary}</p>           </article>         `,       )       .join("");      document.querySelectorAll("[data-line-id]").forEach((card) => {       card.addEventListener("click", () => {         const target = lines.find((line) => line.id === card.dataset.lineId);         if (!target) return;         state.currentLine = target;         lineFeature.renderLine();         requestFeature.renderRequest();         ui.setScreen("line");       });     });      const summaryEl = document.getElementById("home-requests-summary");     if (summaryEl) {       const count = window.HiviAppData.submittedRequests.length;       if (count > 0) {         const pending = window.HiviAppData.submittedRequests.filter(           (r) => r.status !== "Cerrada" && r.status !== "Descartada"         ).length;         summaryEl.innerHTML = `           <div class="activity-card" id="home-requests-link">             <strong>${count} solicitud${count > 1 ? "es" : ""} registrada${count > 1 ? "s" : ""}</strong>             <p>${pending > 0 ? pending + " pendiente" + (pending > 1 ? "s" : "") + " de atencion." : "Todas atendidas."}</p>           </div>         `;       } else {         summaryEl.innerHTML = `           <div class="activity-card" id="home-requests-link">             <strong>Sin solicitudes aun</strong>             <p>Explora las lineas y envia tu primer requerimiento.</p>           </div>         `;       }       const link = document.getElementById("home-requests-link");       if (link) link.addEventListener("click", () => ui.setScreen("requests"));     }   }    window.HiviAppHome = { renderHome }; })();