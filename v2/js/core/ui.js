(function () {   const screens = window.HiviAppData.screens;   const refs = window.HiviAppDOM;   const state = window.HiviAppState;    function updateAppBar(screenKey) {     const config = screens[screenKey] || screens.home;     refs.appbarTitle.textContent = config.title;     refs.appbarKicker.textContent = config.kicker;     refs.appbarAction.textContent = config.action;   }    function updateProfileAvatar(name) {     const initials = name       .split(" ")       .filter(Boolean)       .slice(0, 2)       .map((part) => part.charAt(0).toUpperCase())       .join("");      const avatar = document.querySelector(".profile-avatar");     if (avatar) {       avatar.textContent = initials || "HV";     }   }    function closeMoreSheet() {     refs.moreSheet.classList.remove("is-open");     refs.moreSheet.setAttribute("aria-hidden", "true");     if (refs.moreMenuTrigger) refs.moreMenuTrigger.classList.remove("is-active");   }    function toggleMoreSheet() {     const willOpen = !refs.moreSheet.classList.contains("is-open");     refs.moreSheet.classList.toggle("is-open", willOpen);     refs.moreSheet.setAttribute("aria-hidden", willOpen ? "false" : "true");     if (refs.moreMenuTrigger) refs.moreMenuTrigger.classList.toggle("is-active", willOpen);   }    function syncWebNavToggle() {     const isCollapsed = refs.phoneBody.classList.contains("is-web-nav-collapsed");     refs.webNavToggle.classList.toggle("is-collapsed", isCollapsed);     refs.webNavToggle.setAttribute("aria-expanded", isCollapsed ? "false" : "true");     refs.webNavToggle.setAttribute("aria-label", isCollapsed ? "Mostrar menu" : "Ocultar menu");     refs.webNavToggle.setAttribute("title", isCollapsed ? "Mostrar menu" : "Ocultar menu");   }    function toggleWebNav() {     refs.phoneBody.classList.toggle("is-web-nav-collapsed");     syncWebNavToggle();   }    function setViewMode(mode) {     refs.deviceZone.classList.remove("is-tablet", "is-web");      if (mode === "tablet") {       refs.deviceZone.classList.add("is-tablet");     }      if (mode === "web") {       refs.deviceZone.classList.add("is-web");     }      refs.viewSwitchButtons.forEach((button) => {       button.classList.toggle("is-active", button.dataset.viewMode === mode);     });      if (mode !== "web") {       refs.phoneBody.classList.remove("is-web-nav-collapsed");     }      syncWebNavToggle();   }    const TOOL_SUB_SCREENS = ["unit-converter", "calculators", "formulas", "troubleshooting", "checklist", "brochures", "expert-consult"];    function setScreen(screenKey) {     if (!state.isAuthenticated && screenKey !== "login") {       screenKey = "login";     }      document.querySelectorAll(".screen").forEach((screen) => {       screen.classList.toggle("is-visible", screen.id === `screen-${screenKey}`);     });      const navHighlight = TOOL_SUB_SCREENS.indexOf(screenKey) !== -1 ? "tools" : screenKey;     refs.bottomNavButtons.forEach((button) => {       button.classList.toggle("is-active", button.dataset.screen === navHighlight);     });      closeMoreSheet();     updateAppBar(screenKey);     refs.screenViewport.scrollTop = 0;   }    function setAuthenticated(value) {     state.isAuthenticated = value;     refs.phoneBody.classList.toggle("is-logged-out", !value);     refs.phoneBody.classList.toggle("is-web-nav-collapsed", false);     closeMoreSheet();     updateAppBar(value ? "home" : "login");     syncWebNavToggle();   }    window.HiviAppUI = {     updateProfileAvatar,     closeMoreSheet,     toggleMoreSheet,     setViewMode,     toggleWebNav,     setScreen,     setAuthenticated,   }; })();