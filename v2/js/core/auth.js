(function () {   const refs = window.HiviAppDOM;   const ui = window.HiviAppUI;    function formatName(value) {     return value       .split(" ")       .filter(Boolean)       .map((part) => part.charAt(0).toUpperCase() + part.slice(1).toLowerCase())       .join(" ");   }    function showRegStep(step) {     const panel1 = document.getElementById("reg-panel-1");     const panel2 = document.getElementById("reg-panel-2");     const steps  = document.querySelectorAll(".reg-step");     if (!panel1 || !panel2) return;     if (step === 1) {       panel1.removeAttribute("hidden");       panel2.setAttribute("hidden", "");     } else {       panel1.setAttribute("hidden", "");       panel2.removeAttribute("hidden");     }     steps.forEach((el, i) => el.classList.toggle("is-active", i < step));   }    function setAuthMode(mode) {     refs.authModeButtons.forEach((button) => {       button.classList.toggle("is-active", button.dataset.authMode === mode);     });      refs.authForms.forEach((form) => {       form.classList.toggle("is-visible", form.id === `${mode}-form`);     });      if (mode === "register") showRegStep(1);   }    function setUser(name) {     const normalized = formatName(name || "Cliente Hivimar");     const firstName = normalized.split(" ")[0];     refs.homeName.textContent = firstName;     refs.doneClient.textContent = normalized;     refs.profileName.textContent = normalized;     ui.updateProfileAvatar(normalized);   }    function logout() {     ui.setAuthenticated(false);     setAuthMode("login");     ui.setScreen("login");   }    window.HiviAppAuth = {     setAuthMode,     showRegStep,     setUser,     logout,   }; })();