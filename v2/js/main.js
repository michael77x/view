(function () {   const lines = window.HiviAppData.lines;   const refs = window.HiviAppDOM;   const state = window.HiviAppState;   const auth = window.HiviAppAuth;   const ui = window.HiviAppUI;   const homeFeature = window.HiviAppHome;   const lineFeature = window.HiviAppLine;   const optionsFeature = window.HiviAppOptions;   const requestsFeature = window.HiviAppRequests;   const requestFeature = window.HiviAppRequestForm;    const TOOL_SCREENS = ["unit-converter", "calculators", "formulas", "troubleshooting", "checklist", "brochures", "expert-consult"];    function wireEvents() {     refs.bottomNavButtons.forEach((button) => {       button.addEventListener("click", () => ui.setScreen(button.dataset.screen));     });      refs.viewSwitchButtons.forEach((button) => {       button.addEventListener("click", () => ui.setViewMode(button.dataset.viewMode));     });      refs.authModeButtons.forEach((button) => {       button.addEventListener("click", () => auth.setAuthMode(button.dataset.authMode));     });      document.querySelectorAll("[data-screen-target]").forEach((button) => {       button.addEventListener("click", () => ui.setScreen(button.dataset.screenTarget));     });      if (refs.moreMenuTrigger) refs.moreMenuTrigger.addEventListener("click", ui.toggleMoreSheet);     refs.moreSheetBackdrop.addEventListener("click", ui.closeMoreSheet);     refs.moreSheetClose.addEventListener("click", ui.closeMoreSheet);     refs.webNavToggle.addEventListener("click", ui.toggleWebNav);      refs.appbarAction.addEventListener("click", () => {       const activeScreen = document.querySelector(".screen.is-visible")?.id?.replace("screen-", "") || "home";        if (activeScreen === "profile") {         ui.setScreen("home");         return;       }        if (activeScreen === "done") {         ui.setScreen("home");         return;       }        if (activeScreen === "line") {         ui.setScreen("home");         return;       }        if (activeScreen === "request") {         ui.setScreen("home");         return;       }        if (activeScreen === "tools") {         ui.setScreen("home");         return;       }        if (TOOL_SCREENS.indexOf(activeScreen) !== -1) {         ui.setScreen("tools");         return;       }        if (activeScreen === "requests") {         ui.setScreen("request");         return;       }        if (activeScreen === "home") {         ui.setScreen("tools");       }     });      refs.loginForm.addEventListener("submit", (event) => {       event.preventDefault();       const email = refs.loginEmail.value.trim();       const rawName = email ? email.split("@")[0].replace(/[._-]+/g, " ") : "Cliente Hivimar";       auth.setUser(rawName);       ui.setAuthenticated(true);       ui.setScreen("home");     });      refs.registerForm.addEventListener("submit", (event) => {       event.preventDefault();       const name = refs.clientName.value.trim() || "Cliente Hivimar";       auth.setUser(name);       ui.setAuthenticated(true);       ui.setScreen("home");     });      const regNextBtn = document.getElementById("reg-next-btn");     if (regNextBtn) regNextBtn.addEventListener("click", () => auth.showRegStep(2));      const regBackBtn = document.getElementById("reg-back-btn");     if (regBackBtn) regBackBtn.addEventListener("click", () => auth.showRegStep(1));      refs.toRequestButton.addEventListener("click", () => {       requestFeature.renderRequest();       ui.setScreen("request");     });      refs.requestLine.addEventListener("change", (event) => {       const target = lines.find((line) => line.id === event.target.value);       if (!target) return;       state.currentLine = target;       lineFeature.renderLine();       requestFeature.renderRequest();     });      refs.requestForm.addEventListener("submit", (event) => {       event.preventDefault();       refs.doneLine.textContent = state.currentLine.title;       refs.doneBrand.textContent = refs.requestBrand.value;       requestsFeature.addSubmittedRequest({         type: "cotizacion",         line: state.currentLine.title,         brand: refs.requestBrand.value,         status: "Recibida",         detail: "Solicitud registrada y derivada al responsable correspondiente.",       });       requestsFeature.renderRequests();       ui.setScreen("done");     });      refs.logoutButton.addEventListener("click", auth.logout);   }    function initialize() {     homeFeature.renderHome();     lineFeature.renderLine();     optionsFeature.renderTools();     requestsFeature.renderRequests();     requestFeature.renderRequest();     window.HiviAppUnitConverter.init();     window.HiviAppCalculators.init();     window.HiviAppFormulas.init();     window.HiviAppTroubleshooting.init();     window.HiviAppChecklist.init();     window.HiviAppBrochures.init();     window.HiviAppExpertConsult.init();     auth.setUser("Carlos Mendoza");     auth.setAuthMode("login");     ui.setAuthenticated(false);     ui.setViewMode("mobile");     wireEvents();     ui.setScreen("login");   }    initialize(); })();