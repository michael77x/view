(function () {   const tools = window.HiviAppData.tools;   const ui = window.HiviAppUI;    function renderTools() {     const primary = tools.filter((t) => t.section === "primary");     const secondary = tools.filter((t) => t.section === "secondary");      const toolList = document.getElementById("tool-list");     toolList.innerHTML = primary       .map(         (tool) => `           <article class="tool-card tool-card--active" data-tool-screen="${tool.screen}">             <strong>${tool.title}</strong>             <p>${tool.description}</p>           </article>         `,       )       .join("");      const secondaryList = document.getElementById("secondary-tool-list");     secondaryList.innerHTML = secondary       .map(         (tool) => `           <button class="secondary-tool-item" data-tool-screen="${tool.screen}" type="button">             <span class="sti-title">${tool.title}</span>             <span class="sti-desc">${tool.description}</span>             <svg class="sti-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>           </button>         `,       )       .join("");      document.querySelectorAll("[data-tool-screen]").forEach((el) => {       el.addEventListener("click", () => ui.setScreen(el.dataset.toolScreen));     });   }    window.HiviAppOptions = { renderTools }; })();