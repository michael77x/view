(function () {   function switchCalc(name) {     document.querySelectorAll(".calc-panel").forEach(function (p) {       p.classList.toggle("is-hidden", p.id !== "calc-panel-" + name);     });     document.querySelectorAll("#screen-calculators .calc-tab").forEach(function (t) {       t.classList.toggle("is-active", t.dataset.calc === name);     });     runCalc(name);   }    function runCalc(name) {     if (name === "tdh") calcTDH();     else if (name === "airLeak") calcAirLeak();     else if (name === "steam") calcSteam();   }    function val(id) {     var el = document.getElementById(id);     return el ? parseFloat(el.value) || 0 : 0;   }    function calcTDH() {     var hs = val("calc-tdh-hs");     var hd = val("calc-tdh-hd");     var hf = val("calc-tdh-hf");     var pd = val("calc-tdh-pd");     var tdh = hd + hs + hf + pd;     var el = document.querySelector("#calc-result-tdh .calc-result-value");     if (el) el.textContent = tdh.toFixed(1) + " m";   }    function calcAirLeak() {     var d = val("calc-air-diameter") / 1000;     var p = val("calc-air-pressure");     var h = val("calc-air-hours");     var cost = val("calc-air-cost");     var area = Math.PI * Math.pow(d / 2, 2);     var flowRate = area * 0.65 * (p + 1.01325) * 100000 / 101325 * 3600;     var powerKw = flowRate / 5 * 0.25;     var annual = powerKw * h * cost;     var el = document.querySelector("#calc-result-airLeak .calc-result-value");     if (el) {       el.textContent = "~" + annual.toFixed(0) + " USD/ano (~" + powerKw.toFixed(2) + " kW)";     }   }    function calcSteam() {     var flow = val("calc-steam-flow");     var cp = val("calc-steam-cp");     var tin = val("calc-steam-tin");     var tout = val("calc-steam-tout");     var enthalpy = val("calc-steam-enthalpy");     var q = flow * cp * (tout - tin);     var steam = enthalpy > 0 ? q / enthalpy : 0;     var el = document.querySelector("#calc-result-steam .calc-result-value");     if (el) {       el.textContent = steam.toFixed(1) + " kg/h (" + (q / 1000).toFixed(1) + " MW termico)";     }   }    function init() {     document.querySelectorAll("#screen-calculators .calc-tab").forEach(function (tab) {       tab.addEventListener("click", function () {         switchCalc(tab.dataset.calc);       });     });      ["calc-tdh-hs", "calc-tdh-hd", "calc-tdh-hf", "calc-tdh-pd"].forEach(function (id) {       var el = document.getElementById(id);       if (el) el.addEventListener("input", calcTDH);     });      ["calc-air-diameter", "calc-air-pressure", "calc-air-hours", "calc-air-cost"].forEach(function (id) {       var el = document.getElementById(id);       if (el) el.addEventListener("input", calcAirLeak);     });      ["calc-steam-flow", "calc-steam-cp", "calc-steam-tin", "calc-steam-tout", "calc-steam-enthalpy"].forEach(function (id) {       var el = document.getElementById(id);       if (el) el.addEventListener("input", calcSteam);     });      calcTDH();     calcAirLeak();     calcSteam();   }    window.HiviAppCalculators = { init: init }; })();